name: Learn GitHub Actions
on: [push]

jobs:
  explore-github-actions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: List files
        run: ls -la
      
      - name: Print message
        run: echo "🎉 Мой первый workflow работает!"
